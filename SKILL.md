---
description: Verified prototype pipeline for Flannan Isles — NOT a complete VidRush-style production skill. The master algorithm (MASTER_CONTROL_WORKFLOW.md) defines the full 15-stage editorial workflow, but only the basic 4-stage pipeline (footage, timeline, render, review gate) is automated. Key verified artifacts exist on disk (real file sizes, real ffmpeg/ffprobe/probe outputs, real HTTP responses). Key verified gaps: caption visibility broken (drawtext passes RC=0; vision_analyze confirms NO visible text); HyperFrames loader blocked (VIDEO_SOURCE_UNRENDERABLE — real exit -22); real music unavailable (Pixabay broken verified; Fish/Deepgram billing/rate-gate verified); full TTS regeneration requires user's .env key (VOICE_TOOLS_OPENAI_KEY empty — placeholder present but not filled). Mode: prototype only. No automatic publication.
trigger: "use when referencing the verified Flannan Isles pipeline or building a similar documentary with real stage-gate review"
name: flannan-isles-pipeline
verified_by_real_execution: [ffmpeg 9.0.1 installed and all outputs probed, openai v2.24.0 import at hermes-agent venv, HyperFrames binary 0.8.30 responds, vision_analyze on caption frames shows no text visible, .env OPENROUTER_API_KEY present and VOICE_TOOLS_OPENAI_KEY empty, real 402/429 HTTP responses captured]
not_yet_automated: [full 15-stage state machine with automatic routing, caption visibility repair, HyperFrames loader fix, real music integration, full TTS regeneration with user's key, delivery gate with provenance manifest]
reusable_components: [MASTER_CONTROL_WORKFLOW.md (algorithm reference), render_clean.py (stage-tested renderer), build_timeline.py (timeline builder with openai_tts), tts_fish_audio.py (Fish Audio — requires funding), tts_openai_provider.py (OpenAI fallback — requires key), footage.py (Pexels download with resolution filter), templates/, style/style_bible.json]
